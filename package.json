{
  "name": "followflow-ai",
  "version": "0.1.0",
  "private": true,
  "description": "FollowFlow AI autonomous commercial follow-up SaaS.",
  "scripts": {
    "dev": "pnpm --filter @followflow/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "prettier --check ."
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.28.1",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.30.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "@eslint/js": "^9.30.0",
    "typescript-eslint": "^8.35.0"
  }
}
